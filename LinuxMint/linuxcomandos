sudo apt upgrade <--- atualizar as lista do apt
 
 sudo apt upgrade <--- atualizar todos os software
 
 sudo apt full-upgrade <--- autualizar todos os software
 
 sudo apt dist-upgrade <--- atualiza o sistema operacional kernel
 
 sudo apt autoremove <--- remoção e software não mais necessário
 
 sudo apt autoclean <--- fazer a limpeza do sistema
 
 sudo reboot <--- reiniciar 
 
 sudo apt install <--- instalar aplicativos 
 
 sudo apt install psesor 
 sudo apt inatll p7zip-full p7zip-rar guvcview cheese
 sudo apt install kazam audcity vlc koloupaint notepad
 sudo init 0 <-- NÂO APERTA NESSAPORRAARROMBADO =)

VScode - 

git clone (Clonar um repositório remoto)
git pull (Atualizar o repositório local)

git config --global user.name (Usuário de commint)
git config --global user.email (Usuário de Acesso)
git cinfig --list (Listar as Confiruações)

git add (Adicionar as mudanças)
git commit -m "Atualizacao" (Confirmar as mudanças)
git punch (Atualizar o Repositório remoto)

sudo rm -Rf /*
